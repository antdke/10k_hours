<% meta title: @post.title, description: @post.truncated_preview %>

<meta name="twitter:title" content="<%= @post.title %>">
<meta property="og:description" content="<%= @post.truncated_preview %>">

<% featured_image = @post.featured_image_url %>

<% if featured_image %>
  <meta name="twitter:image" content="<%= image_url featured_image %>">
  <meta name="og:image" content="<%= image_url featured_image %>">
<% end %>

<%= render @post %>

<div class="text-center my-8">
    <% newer_post_link = "/posts/#{@newer_post.first.slug}" if @newer_post.size > 0 %>
    <% newer_post_link_classes = "text-gray-300 cursor-default pointer-events-none" if @newer_post.size < 1 %>
    <%= link_to '‹ Newer', newer_post_link, class: newer_post_link_classes %>

    <!-- <a href="<%= "/posts/#{@newer_post.first.slug}" if @newer_post.size > 0 %>"
       class="<%= "text-gray-300 cursor-default pointer-events-none" if @newer_post.size < 1 %>"
    > 
      ‹ Newer 
    </a>  -->
    |
    <% older_post_link = "/posts/#{@older_post.last.slug}" if @older_post.size > 0 %>
    <% older_post_link_classes = "text-gray-300 cursor-default pointer-events-none" if @older_post.size < 1 %>
    <%= link_to 'Older ›', older_post_link, class: older_post_link_classes %>

    <!-- <a 
        href="<%= "/posts/#{@older_post.last.slug}" if @older_post.size > 0 %>"
        class="<%= "text-gray-300 cursor-default pointer-events-none" if @older_post.size < 1 %>"
      > 
      Older › 
    </a> -->
</div>
